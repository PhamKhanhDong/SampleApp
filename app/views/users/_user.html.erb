<li>
  <%= gravatar_for user, size: Settings.user.name.max_length %>

  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".btn_delete"), user, method: :delete,
    data: { confirm: t(".txt_you_sure") } %>
  <% end %>
</li>
